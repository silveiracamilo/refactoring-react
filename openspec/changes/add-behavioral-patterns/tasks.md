## 1. Routing & Navigation Setup

- [x] 1.1 Adicionar os 10 novos enum values para Behavioral patterns em `RouterPaths.ts`
- [x] 1.2 Adicionar entradas de menu com children para Behavioral em `siderMenu/data.ts`

## 2. Chain of Responsibility

- [x] 2.1 Criar estrutura de feature `behavioral/features/chainOfResponsibility/` com `ChainOfResponsibility.tsx`, `index.tsx`, `loadable.tsx`
- [x] 2.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Chain of Responsibility
- [x] 2.3 Criar `sample/ExampleChainOfResponsibility.tsx` e `sample/exampleChainOfResponsibilityCode.ts`

## 3. Command

- [x] 3.1 Criar estrutura de feature `behavioral/features/command/` com `Command.tsx`, `index.tsx`, `loadable.tsx`
- [x] 3.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Command
- [x] 3.3 Criar `sample/ExampleCommand.tsx` e `sample/exampleCommandCode.ts`

## 4. Iterator

- [x] 4.1 Criar estrutura de feature `behavioral/features/iterator/` com `Iterator.tsx`, `index.tsx`, `loadable.tsx`
- [x] 4.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Iterator
- [x] 4.3 Criar `sample/ExampleIterator.tsx` e `sample/exampleIteratorCode.ts`

## 5. Mediator

- [x] 5.1 Criar estrutura de feature `behavioral/features/mediator/` com `Mediator.tsx`, `index.tsx`, `loadable.tsx`
- [x] 5.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Mediator
- [x] 5.3 Criar `sample/ExampleMediator.tsx` e `sample/exampleMediatorCode.ts`

## 6. Memento

- [x] 6.1 Criar estrutura de feature `behavioral/features/memento/` com `Memento.tsx`, `index.tsx`, `loadable.tsx`
- [x] 6.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Memento
- [x] 6.3 Criar `sample/ExampleMemento.tsx` e `sample/exampleMementoCode.ts`

## 7. Observer

- [x] 7.1 Criar estrutura de feature `behavioral/features/observer/` com `Observer.tsx`, `index.tsx`, `loadable.tsx`
- [x] 7.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Observer
- [x] 7.3 Criar `sample/ExampleObserver.tsx` e `sample/exampleObserverCode.ts`

## 8. State

- [x] 8.1 Criar estrutura de feature `behavioral/features/state/` com `State.tsx`, `index.tsx`, `loadable.tsx`
- [x] 8.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para State
- [x] 8.3 Criar `sample/ExampleState.tsx` e `sample/exampleStateCode.ts`

## 9. Strategy

- [x] 9.1 Criar estrutura de feature `behavioral/features/strategy/` com `Strategy.tsx`, `index.tsx`, `loadable.tsx`
- [x] 9.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Strategy
- [x] 9.3 Criar `sample/ExampleStrategy.tsx` e `sample/exampleStrategyCode.ts`

## 10. Template Method

- [x] 10.1 Criar estrutura de feature `behavioral/features/templateMethod/` com `TemplateMethod.tsx`, `index.tsx`, `loadable.tsx`
- [x] 10.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Template Method
- [x] 10.3 Criar `sample/ExampleTemplateMethod.tsx` e `sample/exampleTemplateMethodCode.ts`

## 11. Visitor

- [x] 11.1 Criar estrutura de feature `behavioral/features/visitor/` com `Visitor.tsx`, `index.tsx`, `loadable.tsx`
- [x] 11.2 Criar `build/` (anchorsBuild, prosConsBuild, sourcesBuild) para Visitor
- [x] 11.3 Criar `sample/ExampleVisitor.tsx` e `sample/exampleVisitorCode.ts`

## 12. Router Integration

- [x] 12.1 Importar e registrar os 10 loadables no `Router.tsx` com as novas rotas

## 13. Validation

- [x] 13.1 Verificar que `npm run build` passa sem erros
- [x] 13.2 Testar navegação no menu para cada um dos 10 padrões
