import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
    hashed: false,
    token: {
        colorTextBase: '#FFFFFF',
        colorBgBase: '#000000',
    }
}

export default theme;
