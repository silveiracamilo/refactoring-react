import { defineMessages } from "react-intl";

const commons = 'shared.commons';

const messages = defineMessages({
    language: {
        id: `${commons}.language`,
        defaultMessage: '*Language',
    }
});

export default messages;
